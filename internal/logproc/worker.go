package logproc

import(
	"fmt"
)


func RunEngine(dirpath string, target string, workers int) (int, error) {
	fmt.Printf("worker received, dirpath: %s, target: %s, workers: %d\n", dirpath, target, workers)


	return  0, nil
	
}
